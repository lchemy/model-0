{
  "name": "@lchemy/model",
  "version": "0.0.1",
  "description": "",
  "author": "Jesse Zhang (http://jessezhang.me/)",
  "scripts": {
    "prebuild": "del-cli ./dist",
    "build": "tsc -p ./tsconfig.build.json",
    "postbuild": "node ./scripts/copy-package && node ./scripts/copy-dotfiles",
    "lint": "tslint -c tslint.json --project tsconfig.json -t stylish",
    "pretest": "tsc -p ./tsconfig.test.json",
    "test": "mocha --ui bdd --require ./.test-tmp/test/bootstrap ./.test-tmp/test/**/*.spec.js",
    "posttest": "del-cli ./.test-tmp"
  },
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "del-cli": "^0.2.0",
    "mocha": "^3.1.2",
    "tslint": "^3.15.1",
    "typescript": "^2.0.6",
    "typings": "^1.5.0"
  }
}
